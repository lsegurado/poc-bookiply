import thumbUp from '../assets/thumb-up.svg';
import thumbDown from '../assets/thumb-down.svg';

/**Images related to positive / negative feedback */
export const feedbackImages = {
    thumbUp,
    thumbDown
}